// Constants
// App-wide constant values
// Includes: color definitions, string constants, API endpoints (future)
// Configuration values, default settings, and magic numbers
// Keeps hardcoded values in one centralized location
